import { IsArtistDirective } from './is-artist.directive';

describe('IsArtistDirective', () => {
  it('should create an instance', () => {
    const directive = new IsArtistDirective();
    expect(directive).toBeTruthy();
  });
});
